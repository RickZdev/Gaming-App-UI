import { SafeAreaView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const HomeScreen = () => {
  return (
    <SafeAreaView className="bg-yellow-400 flex-1 justify-center items-center">
      <Text className=" text-red-700 text-3xl">Hello ano ba yan</Text>
    </SafeAreaView>
  )
}

export default HomeScreen

const styles = StyleSheet.create({})